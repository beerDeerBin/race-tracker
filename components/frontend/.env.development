# Real backend origins for local development (Tilt stack). The SPA always talks to the real
# services cross-origin (anti-mock); CORS on the services allows http://localhost:5173.
VITE_MANAGEMENT_URL=http://localhost:8083
VITE_PERSISTENCE_URL=http://localhost:8082
VITE_REALTIME_URL=http://localhost:8084
